web: uvicorn app.main:app --host 0.0.0.0 --port 8000 --log-level info
# Optional: use gunicorn for higher concurrency in production
# web: gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
