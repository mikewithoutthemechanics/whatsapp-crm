import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.whatsappcrm.sa',
  appName: 'WhatsApp CRM SA',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
